<%= form_with(model: gamestate, local: true) do |form| %>
  <% if gamestate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gamestate.errors.count, "error") %> prohibited this gamestate from being saved:</h2>

      <ul>
        <% gamestate.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :game_id %>
    <%= form.number_field :game_id %>
  </div>

  <div class="field">
    <%= form.label :hero_id %>
    <%= form.number_field :hero_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
