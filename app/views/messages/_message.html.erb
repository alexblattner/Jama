<%= stylesheet_link_tag 'topics' %>

<div  id="message_<%=message.id%>">
<div class="container darker">
	[<%= message.created_at.strftime("%l:%M %p") %>] <%= message.user.name.split('@')[0].capitalize %>: <%= message.body %>
</div>
</div>