Assign levels to this door:
<br>
<% @door = Door.find_by(id: @door_id) %>


Available Levels:
<% @levels = Level.where(game_id: @game_id) %>
<% @level_names = Array.new %>
<% if !@levels.nil? %>
    <% @levels.each do |level| %>
        <%= link_to level.name, queuelevel_path(@game_id, @door_id, level.id) %>
        <% @level_names.push(level.name) %>
    <% end %>
    <% @door.name %>

<br>
<% else %>
        You don't have any level
        <%= link_to 'Go back and make some levels', addlevel_path(@game_id) %>
        <%= link_to 'Back to the dashboard', leveldashboard_path(@game_id) %>
<% end %>
<% @names = levels_to_names(@door) %>
    Levels currently assigned to this door: 
    <br>
    <% @names.each do |name| %>
        <%= name %>
        <br>
<% end %>
    
<br> 
<%= link_to "Remove the last level", dequeuelevel_path(@game_id, @door_id) %>
<br>
<br>
<br>
<br>
<br>
<br>
<br>
<%= link_to 'Finish adding levels for this door and choose a different door',creategamelogic_path(@game_id) %>
