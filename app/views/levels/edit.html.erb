<h1>Editing Level</h1>

<%= render 'form', level: @level %>

<%= link_to 'Show', @level %> |
<%= link_to 'Back', levels_path %>
<% events = Event.where(game_id: @level.game_id) %>
<% events.each do |event| %>
        <%=  event.name %>
        <%=  event.id %>
<% end %>