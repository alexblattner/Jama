<%= stylesheet_link_tag 'general' %>

<nav class="navbar navbar-expand-lg">
    <ul class="navbar-nav">
        <!-- Brand -->
        <li class="navbar-brand">
            <%= link_to(image_tag("logo.jpg", :height =>"47", :width => "70"), root_path) %>
        </li>
        <!-- Main Menu -->
        <li class="nav-item">
            <%= link_to "Game Center", allgames_path %>
        </li>
        <li class="nav-item">
            <%= link_to "Create New Game", creategame_path %>
        </li>
        <!-- User Activity Function-->
        <% if !logged_in? %>
            <li>
                <%= link_to "Log in", login_path  %>
            </li>
        <% else %>
            <li>
                <%= link_to "Log out", logout_path, method: :delete %>
            </li>
        <% end %>
    </ul>
</nav>
