<%= stylesheet_link_tag 'header' %>

<nav class="head-navbar">
    <ul>
        <li onclick="setTimeout(function(){window.location.reload(false);},50)"><%= link_to(image_tag("logo.jpg", :height =>"47", :width => "70"), root_path) %></li>
        <li><%= link_to "Game Center", allgames_path, :class=>'navbar-nav' %></li>

        <% if logged_in? %>
        <li><%= link_to "Create New Game", creategame_path, :class=>'navbar-nav' %></li>
        <li><%= link_to "Discussion", discussion_path, :class=>'navbar-nav' %></li>
        <%end%>
        <div class="nav-item <% if logged_in? %>log<%end%>" style="float: right;">
        <li>
        <!-- User Activity Function-->
        <% if !logged_in? %>
        <%= link_to "Log in", login_path, :class=>'navbar-nav'  %>
        <% else %>
        <%= link_to "Log out", logout_path, :class=>'navbar-nav', method: :delete %>
        <% end %>
        </li>
        </div>
    </ul>
</nav>
