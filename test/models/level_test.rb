require 'test_helper'
#require 'minitest/autorun'

class LevelTest < ActiveSupport::TestCase
  test "the truth" do
    assert true
  end

  # test "should not save level without name" do
  #  level = Level.new
  #  assert_not level.save, "Saved the level without name"
  # end
end
